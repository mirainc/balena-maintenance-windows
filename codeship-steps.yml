- type: serial
  steps:
  - type: parallel
    steps:
      - service: build
        command: echo "Done"
      - service: test
        command: go test ./...
  # - type: serial
  #   tag: ^(master)
  #   service: build
  #   steps:
  #     - command: make push-docker-hub tag=master
